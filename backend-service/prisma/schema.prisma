datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id            String   @id @default(uuid())
  email         String   @unique
  passwordHash  String
  planType      String   @default("free") // "free" or "paid"
  dailyUsage    Int      @default(0)
  monthlyUsage  Int      @default(0)
  lastResetDate DateTime @default(now()) // used to track when to reset counters
  jobs          Job[]
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model Job {
  id             String     @id @default(uuid())
  userId         String
  user           User       @relation(fields: [userId], references: [id])
  status         String     @default("queued") // queued, processing, completed, failed
  originalFile   String // Storage path/URL to uploaded file
  fileSize       Int
  artifacts      Artifact[]
  processingTime Float?
  error          String?
  createdAt      DateTime   @default(now())
  updatedAt      DateTime   @updatedAt
}

model Artifact {
  id        String   @id @default(uuid())
  jobId     String
  job       Job      @relation(fields: [jobId], references: [id], onDelete: Cascade)
  name      String // e.g., "overlay", "results", "graph"
  path      String // URL or relative path to the artifact
  createdAt DateTime @default(now())
}
